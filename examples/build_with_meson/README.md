# Example Building with Meson

This example [`meson.build`](meson.build) demonstrates how to build an executable that depends on `iguana`.

Note: use the `dependency()` function to include dependencies.
