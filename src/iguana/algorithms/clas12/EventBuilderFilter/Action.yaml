algorithm:
  name: 'clas12::EventBuilderFilter'

actions:
  - name: Filter
    type: filter
    rank: scalar
    inputs:
      - { name: pid, type: int }
  - name: Filter
    type: filter
    rank: vector
    suffix: vec
    inputs:
      - { name: pids, type: std::vector<int>, dimension: 1 }
