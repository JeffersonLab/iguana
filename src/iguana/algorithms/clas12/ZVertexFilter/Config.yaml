clas12::ZVertexFilter:

  # scattered electron cuts
  electron:

    # default cuts
    - default:
      vz: [ -20.0, 20.0 ]

    # RG-A fall2018 inbending
    - runs: [ 4760, 5419 ]
      vz: [ -13.0, 12.0 ]

    # RG-A fall2018 outbending
    - runs: [ 5420, 5674 ]
      vz: [ -18.0, 10.0 ]
